"""Pipeline package — Concern 2: FETCHING + PROCESSING.

Three entry points:
  src.pipeline.fetch  — fetch all sources → data/raw/YYYY-MM-DD.jsonl
  src.pipeline.run    — process raw data  → data/processed/YYYY-MM-DD.jsonl
"""
